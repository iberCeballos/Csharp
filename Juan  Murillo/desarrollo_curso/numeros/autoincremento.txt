using System;
					
public class pasos
{
	public static void Main()
	{
	    // declare steps variable
      int steps = 0;

      // Two steps forward 
      steps = steps + 2;

      // One step back 
      steps--;

      // Print result to the console
      Console.WriteLine(steps);
			
	}
}